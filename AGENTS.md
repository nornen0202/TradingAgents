## AGENTS

기본 작업 대상을 아래 저장소로 고정합니다.

- 기본 저장소: `https://github.com/nornen0202/TradingAgents`
- 기본 리모트: `fork`

다음 작업 시 아래 규칙을 우선 적용합니다.

- `origin`은 참조용으로만 사용하고, 실제 변경/PR/머지는 기본적으로 `fork` 기준으로 수행합니다.
- PR은 `nornen0202/TradingAgents` 레포지토리를 대상으로 생성합니다.
- 브랜치/원격 기본값이 필요하면 `fork`를 우선 사용합니다.
